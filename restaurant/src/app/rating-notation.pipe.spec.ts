import { RatingNotationPipe } from './rating-notation.pipe';

describe('RatingNotationPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingNotationPipe();
    expect(pipe).toBeTruthy();
  });
});
